name: Terraform

on:
  workflow_dispatch:        # permite correr manualmente (Actions -> Run workflow)
  pull_request:
    paths: [ 'infrastructure/week9-sqs/**' ]
  push:
    branches: [ main ]
    paths: [ 'infrastructure/week9-sqs/**' ]

permissions:
  id-token: write
  contents: read
  pull-requests: write

jobs:
  terraform:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: infrastructure/week9-sqs
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME }}
          aws-region: eu-central-1

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.9.0

      - run: terraform fmt -check
      - run: terraform init
      - run: terraform validate
      - run: terraform plan -no-color -out=tfplan
        id: plan

      - name: Comment plan on PR
        if: github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const out = require('child_process')
              .execSync('terraform show -no-color tfplan', { cwd: 'infrastructure/week9-sqs' })
              .toString();
            const body = '### Terraform Plan\n```hcl\n' + out.slice(0, 60000) + '\n```';
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body
            });

      - name: Apply
        if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
        run: terraform apply -auto-approve tfplan
